---
# requirements.yml
# Зависимости для проекта

collections:
  - name: community.general
    version: ">=7.0.0"
  - name: ansible.posix
    version: ">=1.5.0"

roles:
  # Используем роль из репозитория 1
  - src: https://github.com/havebridge/ansible-role-notes-app.git
    scm: git
    version: main
    name: notes-app
